// Locale routing removed — handled client-side via LocaleProvider.
// Middleware kept as a no-op placeholder for future use.
export function middleware() {}

export const config = {
  matcher: [],
};
